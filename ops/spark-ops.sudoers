# the harness Spark cluster ops: page-cache drop and GPU clock lock, no password.
# Installed by the cutover tooling; validated with visudo -cf before install.
USER_PLACEHOLDER ALL=(root) NOPASSWD: /usr/bin/tee /proc/sys/vm/drop_caches
USER_PLACEHOLDER ALL=(root) NOPASSWD: /usr/bin/nvidia-smi
